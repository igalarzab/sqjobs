version: "2"
services:
  sqjobs-base:
    build:
      args:
        uid: ${UID}
        gid: ${GID}
      context: ../..
      dockerfile: build/base/Dockerfile
    image: sqjobs:base
    working_dir: /code
    command: "/bin/true"
